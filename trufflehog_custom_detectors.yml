detectors:
- keywords:
  - SECRET_KEY
  name: secret key detector
  regex:
    secret: '[a-zA-Z0-9]{32}'
  verify:
  - endpoint: http://localhost:8000/
    headers:
    - 'Authorization: Bearer token'
    unsafe: true